<?php

// In a real application, this could be much more complex,
// mapping to controller classes and methods.
// For now, we'll just map URIs to template files.

return [
    '/' => 'partials/home.php',
    // Example of how you would add another page in the future:
    // '/about' => 'partials/about_page.php',
]; 